#include "main.h"
#include <stdio.h>

/**
 * _strlen_recursion - this function returns the length of a string.
 * @s: Variable representing a string.
 */

int _strlen_recursion(char *s, int i)
{
int len = 0;

if (s[i] == '\0')
return (1);
else
l++;

_strlen_recursion(str, i + 1);
}
